## R CMD check results

0 errors | 0 warnings | 0 notes

## Test environments

* local: Linux Mint 22.1, R 4.5.2
  `R CMD build`
  `R CMD check --as-cran`
  `pkgdown::build_site()`
* win-builder: R-devel and R-release

## Release comments

Feature release: adds `ggplot2` visualization and `tidyr`-style wrangling
support for the vector-valued functional data classes introduced in
tf 0.5.0, plus a new vignette. Requires tf (>= 0.5.0), released 2026-07-15.

## Reverse dependencies

No reverse dependencies.
