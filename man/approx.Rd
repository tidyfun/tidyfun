% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approx.R
\name{approx_linear}
\alias{approx_linear}
\alias{approx_spline}
\alias{approx_fill_extend}
\alias{approx_locf}
\alias{approx_nocb}
\title{Inter-/extrapolation for \code{tfd}-objects}
\usage{
approx_linear(x, arg, evaluations)

approx_spline(x, arg, evaluations)

approx_fill_extend(x, arg, evaluations)

approx_locf(x, arg, evaluations)

approx_nocb(x, arg, evaluations)
}
\arguments{
\item{x}{new arg values to approximate the function for}

\item{arg}{the \code{arg} values of the \code{evaluations}}

\item{evaluations}{the function's values at \code{arg}}
}
\description{
These are the currently available \code{evaluator}-functions for \code{tfd}-objects,
which control how the entries are inter-/extrapolated to previously unseen
\code{arg}-values. They all are merely wrappers around \code{\link[zoo:na.fill]{zoo::na.fill()}},
\code{\link[zoo:na.approx]{zoo::na.approx()}}, etc... Note that these are not meant to be called directly --
they are internal functions used by \code{\link[=evaluate.tfd]{evaluate.tfd()}} to do its thing.

The list:
\itemize{
\item \code{approx_linear} for linear interpolation without extrapolation (i.e.,
\code{\link[zoo:na.approx]{zoo::na.approx()}} with \code{na.rm = FALSE})  -- this is the default,
\item \code{approx_spline} for cubic spline interpolation, (i.e., \code{\link[zoo:na.spline]{zoo::na.spline()}}
with \code{na.rm = FALSE}),
\item \code{approx_fill_extend} for linear interpolation and constant extrapolation
(i.e., \code{\link[zoo:na.fill]{zoo::na.fill()}} with \code{fill = "extend"})
\item \code{approx_locf} for "last observation carried forward"  (i.e.,
\code{\link[zoo:na.locf]{zoo::na.locf()}} with \code{na.rm = FALSE} and
\item \code{approx_nocb} for "next observation carried backward" (i.e.,
\code{\link[zoo:na.locf]{zoo::na.locf()}} with \code{na.rm = FALSE, fromLast = TRUE}).
}

For implementing your own, see source code of \code{tidyfun:::zoo_wrapper}.
}
\seealso{
tfd
}
