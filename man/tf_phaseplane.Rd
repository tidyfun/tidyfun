% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phaseplane.R
\name{tf_phaseplane}
\alias{tf_phaseplane}
\title{Phase-plane representation of functional data}
\usage{
tf_phaseplane(f, order = c(1L, 2L), arg = NULL)
}
\arguments{
\item{f}{a univariate \code{tf} object (\code{tfd} or \code{tfb})}

\item{order}{integer vector of length 2: the derivative orders shown on the
x- and y-axis. Defaults to \code{c(1, 2)} (velocity vs. acceleration); \code{0}
means the function itself. Maximal order for \code{\link[tf:tfb_spline]{tf::tfb_spline()}} objects
is 2.}

\item{arg}{optional grid on which to evaluate the derivatives; defaults to
\code{f}'s own grid, see \code{\link[tf:tf_derive]{tf::tf_derive()}}.}
}
\value{
A two-component \code{tf_mv} object (\code{tfb_mv} for \code{tfb} input if both
derivatives can be represented in basis form, otherwise \code{tfd_mv}) with
components named \code{"D<order>"}, e.g. \code{"D1"} and \code{"D2"}.
}
\description{
Turns a univariate \code{tf} vector into a two-component \code{\link[tf:tfd_mv]{tf::tfd_mv()}} /
\code{\link[tf:tfb_mv]{tf::tfb_mv()}} object holding two derivatives of each function, so that
the curves can be displayed as trajectories in the plane spanned by these
derivatives -- a \emph{phase-plane plot} like \code{\link[fda:phaseplanePlot]{fda::phaseplanePlot()}}.
By default, the first derivative (velocity, on the horizontal axis) is
paired with the second derivative (acceleration, on the vertical axis).
Use \code{order = c(0, 1)} for the classical phase portrait of position vs.
velocity.
}
\details{
Derivatives are computed with \code{\link[tf:tf_derive]{tf::tf_derive()}}, i.e. by finite differences
for \code{tfd} objects. Since differencing amplifies noise, phase-plane plots of
raw data are usually only informative for smooth functions (e.g. a
\code{\link[tf:tfb]{tf::tfb()}} representation, or \code{\link[tf:tf_smooth]{tf::tf_smooth()}}ed data) on a fine grid.

The result is a regular \code{tf_mv} object, so it can be plotted with
\code{\link[=tf_ggplot]{tf_ggplot()}} (map it with \code{aes(tf = ...)} and add \code{\link[ggplot2:geom_path]{ggplot2::geom_path()}})
or \code{\link[=autoplot]{autoplot()}}. Use \code{aes(colour = .arg)} or \code{colour_by_arg = TRUE} to
colour the trajectories by their argument value, which is otherwise not
visible in a phase-plane plot.
}
\examples{
library(ggplot2)
arg <- seq(0, 1, length.out = 101)
# sinusoids of varying frequency: the phase plane shows nested ellipses
f <- tfd(t(sapply(1:4, \(k) sin(2 * pi * k * arg))), arg = arg)
pp <- tf_phaseplane(f)
pp
autoplot(pp, colour_by_arg = TRUE)
# position vs. velocity, with tf_ggplot:
d <- data.frame(id = factor(1:4))
d$f <- f
tf_ggplot(d, aes(tf = tf_phaseplane(f, order = c(0, 1)), colour = id)) +
  geom_path() +
  labs(x = "position", y = "velocity")
}
\seealso{
\code{\link[=autoplot.tf_mv]{autoplot.tf_mv()}}, \code{\link[tf:tf_derive]{tf::tf_derive()}}

Other tidyfun visualization:
\code{\link[=autoplot.tf]{autoplot.tf()}},
\code{\link[=autoplot.tf_mv]{autoplot.tf_mv()}},
\code{\link{ggcapellini}},
\code{\link[=gglasagna]{gglasagna()}},
\code{\link{ggspaghetti}}
}
\concept{tidyfun visualization}
