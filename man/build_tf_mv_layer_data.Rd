% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tf-ggplot.R
\name{build_tf_mv_layer_data}
\alias{build_tf_mv_layer_data}
\title{Build long-format data and mapping for a single multivariate (tf_mv) aesthetic}
\usage{
build_tf_mv_layer_data(
  mv,
  mv_quo,
  parsed_aes,
  scalar_col_map,
  layer_idx,
  enriched_data,
  user_arg,
  interpolate,
  mv_type = NULL
)
}
\arguments{
\item{mv}{The evaluated \code{tf_mv} object.}

\item{mv_quo}{The quosure for the aesthetic (used for axis labels / source column).}

\item{scalar_col_map}{Named list: key -> column name in enriched_data}

\item{layer_idx}{Integer index of this layer (for keying layer-level scalar aes)}

\item{enriched_data}{Data frame with original data + pre-evaluated scalar tf columns}

\item{user_arg}{Optional evaluation grid (overrides natural grid)}

\item{interpolate}{Whether to interpolate tf objects to \code{arg}}

\item{mv_type}{\code{"trajectory"}, \code{"facet"}, or \code{NULL} (resolve from \code{d}).}
}
\value{
\code{list(long_data, new_mapping, axis_labels)}.
}
\description{
Two displays, mirroring \code{\link[tf:plot.tf_mv]{tf::plot.tf_mv()}}: \code{"trajectory"} (the planar curve
x(t) vs y(t), default for \code{d == 2}) and \code{"facet"} (value-vs-arg, one group per
curve x component, default otherwise). The caller (a \code{tf_mv} aesthetic mapped
via \code{aes(tf = ...)}) guarantees this is the only tf aesthetic in the layer.
}
\keyword{internal}
