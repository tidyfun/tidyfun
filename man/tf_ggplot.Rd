% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tf-ggplot.R
\name{tf_ggplot}
\alias{tf_ggplot}
\title{Create a tf-aware ggplot}
\usage{
tf_ggplot(data = NULL, mapping = aes(), ..., arg = NULL, interpolate = TRUE)
}
\arguments{
\item{data}{Default dataset to use for plot. If not provided, must be supplied
in each layer added to the plot.}

\item{mapping}{Default list of aesthetic mappings to use for plot. Can include
tf-specific aesthetics like \code{tf}, \code{tf_x}, \code{tf_y}, \code{tf_ymin}, \code{tf_ymax}.}

\item{...}{Other arguments passed to ggplot2 functions.}

\item{arg}{Optional. Evaluation grid for tf objects. If not provided, uses
the default evaluation grid of the tf objects.}

\item{interpolate}{Logical. Should tf objects be interpolated to the evaluation
grid? Defaults to TRUE.}
}
\value{
A tf_ggplot object that inherits from ggplot
}
\description{
\code{tf_ggplot()} creates a ggplot object that can handle tf (functional data) aesthetics.
It works similarly to \code{ggplot()} but automatically transforms tf objects into
long-format data suitable for standard ggplot2 geoms.
}
\details{
tf_ggplot supports the following tf-specific aesthetics:
\itemize{
\item \code{tf}: Maps a tf object to y aesthetic (shorthand for tf_y)
\item \code{tf_x}: Maps a tf object to x aesthetic
\item \code{tf_y}: Maps a tf object to y aesthetic
\item \code{tf_ymin}: Maps a tf object to ymin aesthetic (for ribbons)
\item \code{tf_ymax}: Maps a tf object to ymax aesthetic (for ribbons)
}

When tf aesthetics are used, the data is automatically transformed:
\itemize{
\item tf objects are evaluated on a common grid
\item Each function becomes multiple rows (one per evaluation point)
\item Group identifiers are created to maintain function identity
\item Non-tf columns are replicated appropriately
}
}
\examples{
\dontrun{
# Basic usage
data <- data.frame(id = 1:3, group = c("A", "A", "B"))
data$func <- tf_rgp(3)

# Method 1: tf aesthetic in constructor
tf_ggplot(data, aes(tf = func, color = group)) + geom_line()

# Method 2: tf aesthetic in geom (equivalent)
tf_ggplot(data) + geom_line(aes(tf = func, color = group))

# Confidence bands
tf_ggplot(data) +
  geom_ribbon(aes(tf_ymin = lower, tf_ymax = upper), alpha = 0.3) +
  geom_line(aes(tf = mean_func))
}

}
